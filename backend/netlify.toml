# ARGES Backend — Netlify deployment (serverless functions)
# NOTE: Express apps work on Netlify via the netlify-lambda adapter.
# For production, deploy backend to Render/Railway instead.

[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
