# Production API URL — the deployed Render backend.
# Must be absolute: the Netlify site serves only static files, so a relative
# "/api" would 404 against the CDN instead of reaching the API.
VITE_API_URL=https://arges-vision-api.onrender.com/api
