{
  "name": "arges-vision-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd backend && npm install && npm run build",
    "start": "cd backend && node dist/server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
